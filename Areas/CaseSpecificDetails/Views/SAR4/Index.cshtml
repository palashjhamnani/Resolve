@model Resolve.Models.Case

@foreach (var item in Model.SAR4)
{
    <dl class="row">
        <dt class="col-sm-4">
            @Html.DisplayNameFor(modelItem => item.AbsenceDescription)
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(modelItem => item.AbsenceDescription)
        </dd>
    </dl>
}