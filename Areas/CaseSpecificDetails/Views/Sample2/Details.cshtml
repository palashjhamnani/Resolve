@model Resolve.Models.Case

@foreach (var item in Model.Sample2)
{
    <dl class="row">
        <dt class="col-sm-4">
            @Html.DisplayNameFor(modelItem => item.SampleDescription)
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(modelItem => item.SampleDescription)
        </dd>
    </dl>
}